% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/omiqGTR.R
\name{remove_node}
\alias{remove_node}
\title{Remove node/subtree from a parsed OMIQ gating tree object}
\usage{
remove_node(gt, path, sep = "|", remove_subtree = FALSE)
}
\arguments{
\item{gt}{A \code{GatingTree} object created by \code{parse_omiqgt()}.}

\item{path}{Full path (as returned by \code{tree_paths()}) to the node to remove.}

\item{sep}{Separator for the paths. Defaults to "|".}

\item{remove_subtree}{Also remove the node's descendants? Defaults to \code{FALSE}
(the descendants are kept and re-wired to the parent).}
}
\value{
A new \code{GatingTree} object with the node/subtree removed.
}
\description{
Deletes the node at \code{path}. By default (\code{remove_subtree = FALSE}) only that
single node is removed and its children are re-attached to its parent, taking
the node's place among the parent's children (if the node was a root, its
children become roots). With \code{remove_subtree = TRUE} the node and its whole
subtree are removed.
}
\details{
As in \code{isolate_subtree()}, any per-file gate adjustments that belonged to a
gate that is no longer applied cease to be relevant: they are dropped from the
returned object and a warning is emitted for each affected node.
}
