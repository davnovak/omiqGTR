% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/omiqGTR.R
\name{isolate_subtree}
\alias{isolate_subtree}
\title{Restrict an OMIQ gating tree to one subtree}
\usage{
isolate_subtree(gt, sep = "/", path)
}
\arguments{
\item{gt}{A \code{GatingTree} object created by \code{parse_omiqgt()}.}

\item{sep}{Separator for the paths. Defaults to "/".}

\item{path}{Full path (as returned by \code{tree_paths()}) to the node that
becomes the base of the extracted subtree.}
}
\value{
A new \code{GatingTree} restricted to the requested subtree.
}
\description{
Extracts the subtree whose base is the node at \code{path}. That node takes the
place of the synthetic "(ungated)" root: its own gate is dropped and its
children become the new top-level populations; everything outside the subtree
is removed.
}
\details{
Any per-file gate adjustments (per-file filters) that belonged to a gate that
is no longer applied in the restricted tree cease to be relevant. Such
adjustments are dropped from the returned object, and a warning is emitted for
each affected node.
}
