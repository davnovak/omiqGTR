% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/omiqGTR.R
\name{parse_omiqgt}
\alias{parse_omiqgt}
\title{Parse an \code{.omiqgt} file}
\usage{
parse_omiqgt(path)
}
\arguments{
\item{path}{Path to the \code{.omiqgt} file.}
}
\value{
Object of class \code{c("GatingTree", "dendrogram")}: gating hierarchy as
dendrogram (the file's root populations are joined under a synthetic
\emph{"(ungated)"} root). The parsed model, per-gate membership functions,
and root->node paths are carried in \code{attr(, "model")} and are used by
\code{print()}, \code{plot()}, and \code{gate()}. Standard dendrogram operations
(\code{labels()}, \code{order.dendrogram()}, \code{cut()}, \code{str()}) should  work too.
}
\description{
Parses and \code{.omiqgt} (OMIQ gating hierarchy) file downloaded from an OMIQ
workflow by opening a \emph{Gating} task and pressing \emph{Ctrl+Shift+D}.
}
